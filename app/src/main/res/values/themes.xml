<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Kharcha" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Shown by the system while the app starts; swapped for Theme.Kharcha in MainActivity. -->
    <style name="Theme.Kharcha.Starting" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/brand_teal</item>
        <item name="postSplashScreenTheme">@style/Theme.Kharcha</item>
    </style>
</resources>
